name: veille automatique du mois

on:
  schedule:
    - cron: '5 6 1 * *'       # Tous les 1ers du mois à 6:05 UTC
  workflow_dispatch:

jobs:
  veille:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 📦 Installer Python et les dépendances
        run: |
          pip install --upgrade pip
          pip install requests beautifulsoup4 pandas jupyter

      - name: 📂 Vérifier la présence du dossier veilles
        run: mkdir -p "projets/veille métier/veilles"

      - name: 🚀 Lancer le script
        run: python "projets/veille métier/veuille.py"

      - name: 🔄 Ajouter les fichiers générés dans Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add "projets/veille métier/veilles/*"
          git commit -m "Veille mensuelle automatisée"
          git push
