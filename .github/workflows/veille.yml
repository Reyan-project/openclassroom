name: veille automatique du mois

on:
  schedule:
    - cron: '5 6 1 * *'    # Chaque 1er du mois à 6h05 UTC
  workflow_dispatch:

jobs:
  veille:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 📝 Vérifier la structure du projet (racine)
        run: ls -al

      - name: 📝 Vérifier le dossier projets/veille_metier (debug)
        run: ls -al projets/veille_metier || echo "Pas de dossier projets/veille_metier"

      - name: 📦 Installer Python et les dépendances
        run: |
          pip install --upgrade pip
          pip install requests beautifulsoup4 pandas csv datetime os

      - name: 📂 Créer le dossier veilles si absent
        run: mkdir -p projets/veille_metier/veilles

      - name: 🚀 Lancer le script principal
        run: python projets/veille_metier/veuille.py

      - name: 📝 Lister les fichiers générés
        run: ls -al projets/veille_metier/veilles

      - name: 🔄 Ajouter les fichiers générés dans Git SI des fichiers existent
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          FILES=$(find projets/veille_metier/veilles -type f | wc -l)
          if [ "$FILES" -gt "0" ]; then
            git add projets/veille_metier/veilles/*
            git commit -m "Veille mensuelle automatisée"
            git push
          else
            echo "Aucun fichier à commiter dans projets/veille_metier/veilles"
          fi
